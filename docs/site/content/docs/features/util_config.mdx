---
# Generated by `python scripts/cb.py docs-sync` from scripts/spec.py.
# Edit the prose below, not this block — the sync rewrites it and
# `docs-sync --check` fails CI when it drifts.
title: "Admin configuration menu"
description: "QA says /config, v1 ships /configurar - both must resolve"
status: done
milestone: M1
area: util
layer: gateway
triggers: ["/config", "/configure", "/configurar"]
v1_source: "Configurations.py:139-167"
contract: "docs/contracts/util_config.md"
---
<FeatureHeader id="util_config" />

## What it does

{/* One paragraph, from the user's side. What happens in the chat? */}

## Behaviour that must not change

{/* v1 compatibility is not negotiable: the aliases, the wording, the
    permissions. Anything a group would notice if it moved. */}

The full behaviour contract lives in [`docs/contracts/util_config.md`](https://github.com/Cookiebot-Team/cookiebot-telegram-bot/blob/main/docs/contracts/util_config.md).

## How to verify it

{/* Which scenario file, and what to do by hand in the sandbox. */}
