---
# Generated by `python scripts/cb.py docs-sync` from scripts/spec.py.
# Edit the prose below, not this block — the sync rewrites it and
# `docs-sync --check` fails CI when it drifts.
title: "Block listed users from joining"
description: "external cas.chat + burrbot need timeout and circuit breaker"
status: done
milestone: M1
area: util
layer: gateway
v1_source: "GroupShield.py:172-229"
contract: "docs/contracts/util_doomlist.md"
---
<FeatureHeader id="util_doomlist" />

## What it does

{/* One paragraph, from the user's side. What happens in the chat? */}

## Behaviour that must not change

{/* v1 compatibility is not negotiable: the aliases, the wording, the
    permissions. Anything a group would notice if it moved. */}

The full behaviour contract lives in [`docs/contracts/util_doomlist.md`](https://github.com/Cookiebot-Team/cookiebot-telegram-bot/blob/main/docs/contracts/util_doomlist.md).

## How to verify it

{/* Which scenario file, and what to do by hand in the sandbox. */}
