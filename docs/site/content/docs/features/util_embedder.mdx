---
# Generated by `python scripts/cb.py docs-sync` from scripts/spec.py.
# Edit the prose below, not this block — the sync rewrites it and
# `docs-sync --check` fails CI when it drifts.
title: "Rewrite social links"
description: "rewrites the 3 hosts v1 actually rewrites; vm.tiktok.com short links need a redirect resolve"
status: done
milestone: M2
area: util
layer: gateway
v1_source: "SocialContent.py:79-84"
contract: "docs/contracts/util_embedder.md"
---
<FeatureHeader id="util_embedder" />

## What it does

{/* One paragraph, from the user's side. What happens in the chat? */}

## Behaviour that must not change

{/* v1 compatibility is not negotiable: the aliases, the wording, the
    permissions. Anything a group would notice if it moved. */}

The full behaviour contract lives in [`docs/contracts/util_embedder.md`](https://github.com/Cookiebot-Team/cookiebot-telegram-bot/blob/main/docs/contracts/util_embedder.md).

## How to verify it

{/* Which scenario file, and what to do by hand in the sandbox. */}
