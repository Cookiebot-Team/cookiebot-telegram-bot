---
# Generated by `python scripts/cb.py docs-sync` from scripts/spec.py.
# Edit the prose below, not this block — the sync rewrites it and
# `docs-sync --check` fails CI when it drifts.
title: "Language selection"
description: "QA describes a web settings page; v1 only has the in-chat menu"
status: done
milestone: M1
area: core
layer: gateway
v1_source: "Configurations.py:242-251"
contract: "docs/contracts/core_setlang.md"
---
<FeatureHeader id="core_setlang" />

## What it does

{/* One paragraph, from the user's side. What happens in the chat? */}

## Behaviour that must not change

{/* v1 compatibility is not negotiable: the aliases, the wording, the
    permissions. Anything a group would notice if it moved. */}

The full behaviour contract lives in [`docs/contracts/core_setlang.md`](https://github.com/Cookiebot-Team/cookiebot-telegram-bot/blob/main/docs/contracts/core_setlang.md).

## How to verify it

{/* Which scenario file, and what to do by hand in the sandbox. */}
