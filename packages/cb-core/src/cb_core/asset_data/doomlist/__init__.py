"""v1's `Bot/Static/silence_scammer.jpg`, copied byte-identically.

Posted after a join-time ban check fires, one time in ten
(`../COOKIEBOT-Telegram-Group-Bot/Bot/COOKIEBOT.py:143-145`). See
`cb_core/skins.py` for the gate that decides whether a given skin posts it.
"""
